# 측정기가 한 문형만 재고 있었다 (2026-09-04)

`도코에`(どこへ, 어디로)가 `と`(조사) + `こえ`(목소리)로 갈려, 되살린 원문이
`とこえ` 라는 **말이 안 되는 문자열**이 되어 번역기로 넘어갔다. 점수 차는 6점이었다.

    [도][코에]   と 100.0 + こえ 66.2  = 166.2   ← 이기고 있었다
    [도코][에]   どこ 80.0 + へ  80.4  = 160.4

앞뒤를 보는 규칙("조사 앞에는 낱말이 온다")이 필요하다는 것까지는 전에 적어 두었는데,
그 규칙을 걸어 봐도 **300문장 수치가 한 자리도 안 움직인다**는 기록도 함께 있었다.
그 기록이 왜 그랬는지부터 밝히는 일이 이번 작업이었다.

## 표본이 한 문형이었다

`SegmentCaseBuilder` 가 Tanaka Corpus 를 **앞에서부터** 읽어 300개를 채우고 있었다.
말뭉치는 일본어 문장으로 정렬돼 있고 앞머리가 `彼は忙しい…` 한 덩어리다.

    300개 중 290개가 `彼` 로 시작했다.

첫 조각을 두고 다투는 문장이 표본에 아예 없었으니, 첫 조각 규칙이 수치를 못 움직이는
것은 당연했다. **재서 손해인 길이 아니라, 잴 수 없는 길이었다.**

`は`·`へ` 를 소리대로 못 치던 때와 같은 병이다(2026-09-03). 그때도 케이스가 그 입력을
하나도 만들어 내지 않고 있었다. **측정기가 못 보는 자리는 성적표에 0으로도 안 찍힌다.**

고친 방식은 둘이다.

1. 쓸 수 있는 문장을 **다 모은 뒤 균등 간격으로** 집는다. 난수를 쓰지 않아 돌릴 때마다
   같은 표본이 나온다.
2. **주석이 문장을 다 덮는지 A 줄과 맞춰 본다.** Tanaka 의 B 줄은 사전에 실린 낱말만
   적어서 고유명사를 건너뛴다 — `次郎は服にさっとブラシをかけた` 의 주석이 `は 服 に …`
   로 시작한다. 그것을 케이스로 만들면 **조사로 시작하는 입력**(`와후쿠니…`)이 되는데,
   그런 문장은 일본어에 없다. 균등 간격으로 집었더니 13개가 그런 것이었다.
   덮지 못하는 줄은 전체의 9%뿐이라 버려도 표본이 넉넉하다.

새 표본의 첫 낱말: `彼` 52 · `私` 29 · `この` 16 · `その` 10 · `君` 8 …

> **CJK 를 `sort | uniq -c` 로 세지 말 것.** 표본을 확인하다 두 번 속았다.
> macOS 의 로케일 처리 때문에 없는 중복이 생긴다("`は` 로 시작 168개"가 실제로는 13개).
> 파이썬 `Counter` 로 다시 셌다.

## 측정기가 앱과 다른 가중치로 재고 있었다

`SpikeRunner` 의 `tunedWeights` 가 `jmdictWeight` 를 **선언보다 먼저** 읽고 있었다.
`main.swift` 의 전역은 적힌 차례대로 초기화되는데, 초기화 전에 읽은 `Double?` 은
0으로 채워진 메모리라 `nil` 이 아니라 **`.some(0.0)`** 으로 읽힌다.

그래서 `--segment-fails` 와 `--bound-sweep` 은 `jmdictWeight 0` 으로, `--segment-sweep`
은 앱과 같은 1 로 재고 있었다. 빈도 목록에 없는 낱말이 통째로 0점이 되는 설정이라
차이가 컸다 — **완전일치가 2.4%p 낮게** 나왔다. 선언을 위로 올려 고쳤다.

## 고친 것 둘

**어절의 첫 조각은 부착어일 수 없다**(`Segmenter.defaultBoundPenalty = 40`).
조사·조동사·계사는 앞에 기댈 자립어가 있어야 한다. 접속사는 빼놓는다 — `でも` 는
문장을 연다. 조사끼리 잇따르는 것(`には`·`では`)도 벌하지 않는다. 실제로 흔하다.

**명사와 부사에는 활용을 되돌려 붙이지 않는다**(`Ranker`). 첫머리 규칙만 걸었더니
`도코에` 가 이번에는 통째로 한 조각 `どうこう`(如何斯う, 이러쿵저러쿵)로 갔다 —
장음 둘을 지어내고 어미를 명령형으로 되돌려 만든 것이다. 명사는 이미 막고 있었는데
부사가 열려 있었다. 둘 다 활용하지 않는 갈래다.

두 규칙이 **함께 있어야** `何処 へ` 가 선다. 하나만으로는 안 된다.

## 조각 비용을 다시 훑었다

135 는 망가진 표본에서 고른 값이었다. 새 표본으로 다시 훑으니 140 이다.

    비용 135   완전일치 45.3%   경계 F1 0.894
    비용 140   완전일치 47.7%   경계 F1 0.897   ← 최적
    비용 145   완전일치 47.0%   경계 F1 0.893

135 를 지키던 유일한 까닭은 `何時`(몇 시)가 `何`+`時` 로 갈리는 것이었는데,
140 도 `今 何時 です か` 로 지킨다. 그래서 훑기가 고른 값을 그대로 쓴다.

첫머리 벌점도 훑어 골랐다(비용 140 아래).

    벌점   0    완전일치 46.7%
    벌점  20    완전일치 47.3%
    벌점  40    완전일치 47.7%   ← 최적
    벌점  80    완전일치 47.0%

40 을 넘겨도 더 좋아지지 않는다. 뒤집을 것은 이미 뒤집혔고, 그 위는 첫 조각을 억지로
길게 만드는 쪽으로만 움직인다.

## 성적

Tanaka 문장 300개(새 표본) · JESC 빈도 · 고친 측정기 기준.

| | 틀린 문장 | 경계 맞힌 조각 | 그중 1위 | 완전일치 |
|---|---|---|---|---|
| 전 (비용 135 · 규칙 없음) | 167/300 | 697 | 92.8% | 44.3% |
| 후 (비용 140 · 벌점 40 · 부사 규칙) | **157/300** | **759** | **93.1%** | **47.7%** |

낱말 케이스 150개는 한 건도 안 움직였다(JPDB 142/150 · JESC 136/150).
치는 동안의 시간도 그대로다(7글자 26ms).

앱 화면으로도 봤다 — `--query=도코에` 가 문장 `どこへ` 와 카드 둘(`どこ` 어디 ·
`へ` ~로)을 낸다.

## 함정 하나 더

`swift build -c release` 는 **`public static let` 의 값만 바뀐 것을 의존 타깃에
옮기지 않는다.** 기본값을 140·40 으로 고치고 다시 빌드했는데 측정기는 계속 135·0 으로
재고 있었다. `touch` 로 강제 재컴파일해야 바뀐다. 기본값을 고친 뒤 성적을 낼 때는
**측정기가 그 값을 쓰고 있는지 로그 줄로 먼저 확인할 것**(`조각 비용 140.0 · … · 첫머리 조사 벌점 40.0`).

## 이어서 할 자리

- 남은 오답 157개를 갈래별로 세어 다음 규칙을 고를 것. 첫머리 규칙은 어절 첫 조각에만
  걸리므로 한 입력에 한 번뿐이다 — 문장 가운데를 보는 규칙은 아직 없다.
- 조사끼리 잇따르는 자리(`には`)를 벌할지 말지는 아직 안 재 봤다. 정답 쪽이 `니하` 를
  한 조각으로 두는 일이 있어(Tanaka), 그 자료부터 세어 보는 것이 먼저다.
- 복합 조사(`ではない`)는 여전히 빈도 목록이 복합 표현을 안 센다는 자료 쪽 문제다.

---

# 이어서 — 조사가 뒷낱말에 삼켜지고 있었다 (2026-09-05)

## 47.7% 는 실력이 아니라 채점표였다

새 규칙을 걸기 전에 **틀린 157건이 무엇인지부터 갈랐다.** 경계 집합의 포함 관계로
갈래를 나누고(정답 경계를 다 품으면 과분할, 일부만 품으면 뭉침, 그 밖은 엇갈림),
과분할은 다시 **깨진 것이 자립어인지**로 갈랐다(사전의 품사 태그로).

    기준 차이(조사·조동사·관용구 경계에서 갈림)   56   화면에서는 안 틀린다
    조사가 삼켜져 딴 낱말이 됨                    48
    자립어를 깬 과분할                            26
    그 밖의 뭉침·엇갈림                           27

56건은 Tanaka 가 묶는 단위가 우리와 다를 뿐이다 — `ですか → です + か` ·
`私の → 私 + の` · `には → に + は`. 카드에는 양쪽 다 제 뜻이 뜬다. 사전이 통째로
실은 구로 뭉친 것(`戸を開ける` · `味がする`)도 마찬가지다.

**그래서 진짜 성적은 199/300(66.3%)이었다.** 채점표 숫자보다 18%p 높다.
이 갈래질은 다음 규칙을 어디에 걸지도 함께 알려 주었다 — 가장 큰 덩어리가
조사 삼킴이고, 그것은 **전날 넣은 규칙의 정확히 반대편**이다.

## 규칙: 조사 앞이 자립어면 그 경계를 값싸게

조각 비용은 어느 경계에나 똑같이 걸린다. 그런데 조사 앞 경계는 다르다 —
조사는 붙어 다니지만 제 몫의 낱말이고, 일본어 문장에서 가장 자주 나는 경계다.

앞 조각이 무엇이었는지를 봐야 하므로 **DP 가 자리마다 점수 하나로는 모자라다.**
마지막 조각의 갈래(`Role`: 첫머리 · 자립어 · 부착어 · 미지)를 함께 들고 다니게
바꿨다. 갈래가 넷뿐이라 상태를 넷으로 늘려도 값이 거의 안 든다(7글자 26 → 32ms).

조사가 어디에 서야 하는지는 이제 함수 하나에 모여 있다.

    앞이 첫머리   → 벌점 40    그것은 조사가 아니다
    앞이 자립어   → 보너스 12  제자리다
    앞이 부착어   → 0          `には`·`では` 는 실제로 흔하다

**조각의 갈래는 1위 후보가 정한다.** 갈래별로 가장 좋은 후보를 따로 고르는 길도
있지만 그러면 규칙이 스스로 무력해진다 — `도` 는 조사 `と`(100)와 명사 `土`(100)를
함께 내므로, 조사에 벌점을 물리면 같은 점수의 명사로 갈아타 벌점을 피한다.

## 잣대 둘이 다른 값을 고른다

    보너스    Tanaka 틀림    진짜 오류
       0        157/300       101/300
       8        153            92
      10        152            91
      12        153            89     ← 고른 값
      18        156            87
      25        161            87
      40        192            91

**진짜 오류 잣대를 그대로 따르지 않았다.** 그 잣대는 "조사를 떼어낸 것은 오류로
안 센다"고 정해 놓은 것이라, 조사 떼기를 키우면 당연히 좋아진다. 자기가 편드는
규칙을 자기가 채점하게 두면 안 된다. 12 는 두 잣대가 다 인정하는 자리다.

40 에서 둘이 함께 무너진다 — 자립어를 깬 문장이 26 → 52 로 늘어난다
(`そんなに` → `そんな`+`に`, `いつも` → `いつ`+`も`).

같은 까닭으로 훑기가 고른 조각 비용 145 · 활용 벌점 10 도 쓰지 않았다.
채점표로는 낫지만(50.7% 대 49.0%) 진짜 오류로는 뒤진다(94 대 89).

## 성적

    Tanaka 틀린 문장  157 → 153 · 경계 맞힌 조각 759 → 772 · F1 0.897 → 0.905
    진짜 오류         101 → 89   (맞은 문장 199 → 211/300)
    그중 조사 삼킴     48 → 32

앱 화면으로도 봤다. `쿄다이가이마스카` 가 카드 넷으로 선다 — `きょだい` · `が` ·
`いる` · `か`. **경계는 이제 맞다.** 남은 것은 낱말 고르기다(`きょだい` 가 兄弟 가
아니라 巨大 로, `いる` 가 居る 가 아니라 要る 로 나온다). 그것은 장음 모호성과
동음이의어 순위 문제라 분절과 다른 일이다.

## 이어서 할 자리

- **남은 89건 중 가장 큰 것은 "그 밖의 뭉침·엇갈림" 29건**이다. 갈래가 아직 안 나뉘어
  있으니 다음은 그것부터 세는 일이다.
- 자립어를 깬 과분할이 26 → 28 로 늘었다. 보너스의 대가다. `そんなに`·`いつも`
  처럼 **조사로 끝나는 부사**가 표적이 되는데, 그 낱말들이 사전에 부사로 실려 있으니
  "부사 안쪽은 자르지 않는다"는 규칙이 가능한지 재 볼 만하다.
- `きょだい` → 巨大 대 兄弟 는 장음(きょうだい)을 지어내는 값의 문제다. 분절이 아니라
  `Ranker` 쪽이다.

---

# 이어서 — 세는 자리가 문제였다 (2026-09-06)

## 남은 오류를 갈랐더니 자료 쪽을 가리켰다

조사 삼킴을 고친 뒤 남은 89건 중 아직 안 갈라 본 "뭉침·엇갈림" 29건을 봤다.
가장 큰 덩어리가 `ではない` 붙이(10건)였고, 그 옆에 `そんなに`·`いつも`·`すぐに` 가
갈린 것들이 있었다. 셋 다 같은 모양이다 — **사전에는 한 낱말인데 빈도 목록에 없다.**

    そんなに   10점 (빈도 목록에 없어 JMdict 태그 점수)
    そんな + に  79.8 + 110.5 = 190.3

점수 차가 이만큼이면 조각 비용으로도 보너스로도 못 뒤집는다. 실제로 재 봤다 —
"붙여 놓은 것이 사전에 있으면 조사 경계 보너스를 안 준다"는 규칙을 넣었더니
**삼킴이 그대로 되살아났다**(`が`+`います` 를 붙인 `買います` 도 사전에 있는 낱말이라
보너스가 꺼진다). 진짜 오류 89 → 96. 되돌렸다.

틀린 153건 가운데 **129건이 정답 낱말 하나가 빈도 목록에 없는 문장**이었다.
점수로 풀 자리가 아니었다.

## 토크나이저가 못 세는 것을 세게 한다

`--build-frequency` 는 macOS 토크나이저가 내놓는 토큰을 하나씩 센다. 그래서
토큰 여럿으로 쪼개지는 표제항은 목록에 아예 안 실린다.

이어진 토큰 둘~넷을 붙여 보고 **사전에 그 낱말이 있을 때만** 한 번 더 센다.
아무 이어짐이나 세면 말뭉치에 없는 낱말이 생기므로, 사전이 아는 (표기·읽기) 짝
322,720개를 미리 모아 두고 그 안에 드는 것만 센다. 가나로 쓰는 낱말과 `uk` 는
읽기가 곧 표기다 — `何時も` 는 자막에 `いつも` 로 적힌다.

    어휘 53,776 → 69,049
    ではない 328위 · には 52위 · どうして 267위 · いつも 330위 · すぐに 480위 · そんなに 815위

## 값이 함께 움직였다

빈도가 바뀌면 최적 가중치도 함께 움직인다. 조사 경계 보너스를 다시 훑었다.

    보너스    Tanaka 틀림    진짜 오류
      12        146/300        87/300
      18        147            85
      20        147            82     ← 고른 값
      22        147            83
      24        151            88
      30        170            94

18~22 가 평평하고 Tanaka 쪽은 그 구간에서 안 움직인다 — 채점표를 내주지 않고 얻는다.

**조각 비용은 안 옮겼다.** 훑기는 155 를 고르고(Tanaka 141/300) 진짜 오류로는
95 로 뒤진다. 140 은 147/300 · 82 다. `そんなに` 는 149 이상이면 붙는데(8.5점 차),
그 하나를 얻자고 뭉침을 열 건 넘게 늘리지 않는다.

## 성적

    Tanaka 틀린 문장  153 → 147 · 경계 F1 0.905 → 0.908 · 완전일치 49.0% → 51.0%
    진짜 오류         83 → 82   (채점기를 함께 고쳐서 잰 값)
    낱말 케이스       136 → 137/150
    치는 동안         7글자 23ms

**진짜 오류가 거의 안 움직인 것은 오류의 성격이 바뀌었기 때문이다** —
과분할 28 → 8 로 줄고 뭉침이 그만큼 늘었다. 그런데 늘어난 뭉침의 상당수가
`ここに`·`車を止める` 처럼 **사전이 통째로 실은 구**라, 카드 하나에 제 뜻이 뜬다.
채점기가 그것을 오류로 세고 있어서 수치가 실제보다 나쁘게 나온다.

## 뜻도 다시 구웠다

목록에 복합 표현이 들어오면서 굽기 대상이 15,437 → 23,055 개가 되었고 구멍이
3,638개 생겼다. 다시 구워 **전 구간 100%** 로 되돌렸다(3,948개 굽기 · 25개는 손으로).

화면에서 보이는 것: `이마난지데스카` 가 `いま`(현재) · `なんじ`(몇 시) ·
`ですか`(관용구 · ~입니까) 세 카드로 선다. 전에는 `です` 와 `か` 가 따로였다.

## 이어서 할 자리

- **채점기부터 고칠 것.** 지금은 사전에 실린 구로 뭉친 것을 오류로 센다. 정답 낱말을
  이어 붙인 문자열이 사전에 있는지로 보는데, 활용형이라 표제형과 안 맞으면 놓친다
  (`車をとめなかった` 대 `車を止める`). 활용을 되돌려 맞춰야 한다.
- `스키데하나이` 는 여전히 `好き` + `は` + `無い` 다. `ではない` 가 328위를 얻었는데도
  안 쓰이는 까닭을 아직 안 봤다.
- `きょだい` → 巨大 대 兄弟 는 장음을 지어내는 값의 문제다. `Ranker` 쪽이다.
